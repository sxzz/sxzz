name: Release

on:
  push:

jobs:
  release:
    uses: sxzz/workflows/.github/workflows/release.yml@main
    with:
      github-release: false
      publish: true
      stage: true
    permissions:
      contents: write
      id-token: write
